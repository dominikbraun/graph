run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - govet
    - gofumpt
    - deadcode

linters-settings:
  govet:
    disable:
      - stdmethods

  gofumpt:
    extra-rules: true
    module-path: github.com/dominikbraun/graph